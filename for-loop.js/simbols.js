function simvols(input){
    let text = input[0];
    for(let i = 0; i < text.length; i++){
        let word = text[i];
       
        console.log(word);
    }

}
simvols(["softuni"]);
simvols(["ice cream"]);